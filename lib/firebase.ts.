// Import the functions you need from the SDKs you need
import { initializeApp, getApps, getApp } from "firebase/app";
import { getFirestore } from "firebase/firestore";
// No necesitamos getAnalytics para la base de datos, así que lo quitamos para simplificar

// Your web app's Firebase configuration
const firebaseConfig = {
  apiKey: "AIzaSyABSWgXESeVRI5FcW0HncGrF07Ya1Gqnos",
  authDomain: "mi-workflow-app.firebaseapp.com",
  projectId: "mi-workflow-app",
  storageBucket: "mi-workflow-app.firebasestorage.app",
  messagingSenderId: "895117243506",
  appId: "1:895117243506:web:ee35a0e412a3040ebe3eeb",
  measurementId: "G-94VH7JEEGD"
};

// Initialize Firebase
// Esta lógica evita que la app se reinicie en cada cambio, que es un error común
const app = !getApps().length ? initializeApp(firebaseConfig) : getApp();
const db = getFirestore(app);

// Exportamos la base de datos (db) para poder usarla en otras partes de la aplicación
export { app, db };